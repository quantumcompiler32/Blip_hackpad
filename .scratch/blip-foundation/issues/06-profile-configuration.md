# 06 — Configure and persist the three stable Profiles

**What to build:** Let Main, Coding, and Research mappings be safely edited, persisted, backed up, and exercised through simulated input.

**Blocked by:** 04 — Prove the simulator-to-Agent tracer bullet; 05 — Prove the bundled Agent lifecycle and private IPC.

**Status:** ready-for-agent

- [ ] SQLite authoritatively stores exactly `main`, `coding`, and `research` with immutable order.
- [ ] SW11 and SW12 mappings cannot be changed by editing or import.
- [ ] Validated JSON import/export omits credentials and preserves unknown action types disabled.
- [ ] Simulated Profile switching updates Agent, Control Center, and display state.
